<div class="container">
  <h1>Create a New Listing</h1>
  <div class="row">
    <div class="col-8">
      <%= simple_form_for [@listing] do |f| %>
      <%= f.input :title %>
      <%= f.input :category, collection: ['couch', 'yoga mat', 'tent'], :input_html => { :class => "select2" } %>
      <%= f.input :address %>
      <%= f.input :price %>
      <%= f.input :description %>
    </div>
    <div class="col-4 mt-3">
      <%= image_tag "", width: 300, class: "hidden", id: "photo-preview" %>
      <%= f.input :photo, input_html: { class: 'hidden d-none', id: 'photo-input'}, label_html: { class: 'upload-photo'}, label: '📷 Upload a photo' %>
      <%= f.input :photo_cache, as: :hidden %>
    </div>
  </div>
  <div class="row">
    <%= f.submit "New Listing", class: "btn btn-primary ml-3", id:"new-listing-button", style: "position: absolute; top: -1000px;" %>
    <% end %>
  <button id="new-listing-alert" class="btn btn-primary btn-block", id="new-listing-alert">Request Booking!</button>
  </div>
</div>
